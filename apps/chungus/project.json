{
  "name": "chungus",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/chungus/src",
  "projectType": "application",
  "targets": {
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "chungus:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "chungus:build:development"
        },
        "production": {
          "buildTarget": "chungus:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/chungus/jest.config.ts"
      }
    }
  },
  "tags": []
}
